-- generated by cds-compiler version 2.11.2
VIEW CDSViews_SupplierOrders AS SELECT
  POrders_0.OrderId,
  POrders_0.SupplierId,
  POrders_0.GrossAmount,
  POrders_0.Status
FROM CDSViews_POrders AS POrders_0
WITH ASSOCIATIONS (
  MANY TO MANY JOIN CDSViews_SupplierOrderItems AS items ON (items.OrderId = OrderId),
  MANY TO ONE JOIN MasterData_Partner AS supplier ON (supplier.PARTNERID = SupplierId)
)